{
  "name": "ran-nuan-tv",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter desktop dev\"",
    "dev:desktop": "pnpm --filter desktop dev",
    "dev:server": "pnpm --filter server dev",
    "dev:mobile": "concurrently \"pnpm --filter server dev\" \"pnpm --filter desktop dev\"",
    "build:desktop": "pnpm --filter desktop build",
    "build:server": "pnpm --filter server build",
    "build:mobile": "pnpm --filter desktop build",
    "serve:all": "cd server && node server.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.5.0"
  }
}
